version: '3'
services:
  pg:
    container_name: pg
    image: "postgres:9.6.5"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: delicacy_user
      POSTGRES_DB: delicacy_database
